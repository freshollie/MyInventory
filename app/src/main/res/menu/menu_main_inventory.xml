<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="uk.ac.coventry.bello.myinventory.activities.MainActivity">
    <item
        android:id="@+id/action_list_to_keep"
        android:icon="@drawable/ic_assignment_returned_white_24dp"
        android:title="@string/create_shopping_list"
        app:showAsAction="ifRoom"/>
    <items
        android:id="@+id/action_sort_by_name"
        android:title="@string/sort_by_name"
        app:showAsAction="never|withText"/>
    <items
        android:id="@+id/action_sort_by_price"
        android:title="@string/sort_by_name"
        app:showAsAction="never|withText"/>
    <items
        android:id="@+id/action_sort_by_quantity"
        android:title="@string/sort_by_name"
        app:showAsAction="never|withText"/>
</menu>
